import aura/io

main {
    name = {
        io/println("What is your name? ");
        // Read a line from the stdin and crashes if an error happens
        io/readln()??
    };

    age = {
        io/println("How old are you? ");
        // Reads a line, if Result.succ pipes to String:parse
        io/readln()?? |> String:parse(Int; _)??
    };

    io/println("Hello, " ++ name ++ "! You're" age:to_string() "years old, right?")
}